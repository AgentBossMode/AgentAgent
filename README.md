# AgentAgent
Currently experimenting via ipynb files. 

# Setup 

1. Docker desktop is needed for code execution module. 
2. Setup the docker image in the docker folder. 
3. Setup .env taking .env.example as a template
4. setup a venv, install dependencies from requirements.txt in root folder. 
5. Preferable python interpreter would be >3.10.x 